{
  "name": "corsfix-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:app": "pnpm --filter app dev",
    "build:app": "pnpm --filter app build",
    "start:app": "pnpm --filter app start",
    "test:app": "pnpm --filter app test",
    "dev:proxy": "pnpm --filter proxy dev",
    "build:proxy": "pnpm --filter proxy build",
    "start:proxy": "pnpm --filter proxy start",
    "test:proxy": "pnpm --filter proxy test",
    "test:sdk": "pnpm --filter corsfix test",
    "build:sdk": "pnpm --filter corsfix build"
  }
}
